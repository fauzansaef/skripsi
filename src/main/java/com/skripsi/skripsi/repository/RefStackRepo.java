package com.skripsi.skripsi.repository;

import com.skripsi.skripsi.entity.RefStack;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RefStackRepo extends JpaRepository<RefStack, Integer> {
}
